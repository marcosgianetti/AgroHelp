import 'package:flutter/material.dart';

class SubmitImage extends StatelessWidget {
  const SubmitImage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
